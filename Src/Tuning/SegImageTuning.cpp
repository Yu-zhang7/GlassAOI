#include "SegImageTuning.h"

namespace Tuning {
	// 示例实现（需要时取消注释）：
	// cv::Mat GetDrawImage(const cv::Mat& src, int threshold_value, int level)
	// {
	//     segImage si;
	//     return si.GetDrawImage(src, threshold_value, level);
	// }
}
